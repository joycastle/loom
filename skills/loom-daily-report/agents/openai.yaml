interface:
  display_name: "Loom Daily Report"
  short_description: "基于真实痕迹生成并写回日报"
  default_prompt: "Use $loom-daily-report to sync loom, generate daily report material, draft the report, and write it back."
policy:
  allow_implicit_invocation: true
