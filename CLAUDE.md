# CLAUDE.md

本项目的 AI 指引与跨工具约定见 @AGENTS.md。

**新用户首次上手 / 整理历史**:打开并逐步执行 [ONBOARDING.md](./ONBOARDING.md)
(面向 AI 的执行剧本:配置 → 采集 → 收编 → 私有云 → 主题分类 → 日常)。

铁律(详见 AGENTS.md):不可逆/外发先确认;凭证只进 `~/.loom/.env` 绝不入库;
原始数据不上云;分类看内容别图快;改代码后跑 `python3 -m pytest tests/test_loom.py`。
